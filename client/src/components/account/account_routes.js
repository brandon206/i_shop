import React from 'react';
import SignIn from './sign_in';
import SignUp from './sign_up';
import Route from 'react-router-dom';

export default (props) => {
    console.log("Account Routes Props: ", props);
    return (
        <h1>Account Routes</h1>
    );
};